<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match *.**>
  @type copy
  <store>
    @type loki
    url "#{ENV['LOKI_URL']}"
    tenant_id "#{ENV['LOKI_TENANT']}"
    flush_interval 5s
    extra_labels {"job":"demo"}
  </store>
  <store>
    @type stdout
  </store>
</match>
